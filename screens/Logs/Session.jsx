import React from 'react';
import {View, Text} from 'react-native';

const Session = ({ route, previous, navigation }) => {
  // console.log(route.params);
  // const session = route.params.session;

  return (
    <View>
      <Text>Session</Text>
    </View>
  );
}

export default Session;